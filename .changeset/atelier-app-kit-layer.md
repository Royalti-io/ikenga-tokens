---
"@ikenga/tokens": minor
---

Add the Atelier app-kit component layer + no-build distribution.

- **New opt-in exports:** `@ikenga/tokens/app-kit.css` (the component layer — currently a base reset; component partials land per the `atelier-design-system` plan) and the generated JS-string exports `@ikenga/tokens/tokens-css` + `@ikenga/tokens/app-kit-css` for no-build srcdoc pkgs (injected as an inline `<style>`; replaces the per-pkg hand-copied CSS-as-JS mirrors that drifted). Generated by `scripts/build-css.mjs` via a single `JSON.stringify` escape.
- **Reconciled tokens (additive)** from the prototype `_shared` superset the locked panes consume: the `data-density` system + chrome heights (`--row-h`/`--tab-h`/`--btn-h*`/`--input-h`, with `[data-density=compact|spacious]`), `--motion-*`/`--ease-*`, `--space-10..20`, an extended type scale (`--text-body-sm/-lg/-micro/-code/-h4/-display*` + `--lead-*`), `--font-body`, `--fg-faint` in the base palette, and `--live`/`--live-soft` + `--agent`/`--agent-soft` in Theme A (and base fallbacks for B/C). These retire the per-pkg `aliasCss` shim.
- **Adopted the Fraunces (display) + JetBrains Mono fonts the shell already loads;** `--font-sans` (Plus Jakarta Sans) is unchanged.
- **No change** to existing `tokens.css` color, type-*size*, or radius values — `./tokens.css` consumers (the marketing site, the shell) are visually unaffected.
